namespace AtomFrameworkCs.Enuns
{
    public enum CsAtomTypes
    {
        /*He adds manual sql for execute */
        SQL,
        /* Aply filters to query */
        WHERE,
        WHERE_LIKE,
        ORDER,
        COLUMNS,
        DB_NAME,
        CLASS,
    }
}